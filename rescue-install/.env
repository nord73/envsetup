# Disk & host
DISK=/dev/sda
HOSTNAME=mail1
TZ=Europe/Stockholm

# ZFS
POOL_R=rpool
POOL_B=bpool
ARC_MAX_MB=2048
ENCRYPT=no

# cloud-init datasources (flow YAML)
CI_DATASOURCES=[ConfigDrive,NoCloud,Ec2]

# Safety
FORCE=0   # set 1 to skip destructive confirmation

# --- SSH / user bootstrap (optional but useful) ---
# Create a user (besides root) and grant sudo
NEW_USER=ansible
NEW_USER_SUDO=1

# Import public keys (any/all of these)
# From GitHub/Launchpad via ssh-import-id:
SSH_IMPORT_IDS="gh:YourGitHubUser"
# Inline public keys (space or newline separated):
SSH_AUTHORIZED_KEYS=""
# Or fetch from URLs (space separated):
SSH_AUTHORIZED_KEYS_URLS="https://example.com/keys.txt"

# SSH daemon policy
PERMIT_ROOT_LOGIN=prohibit-password   # yes|no|prohibit-password
PASSWORD_AUTH=no                      # yes|no
